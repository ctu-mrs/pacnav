takeoff:

  takeoff_height: 2.0

  during_takeoff:
    controller: "Se3Controller"

  after_takeoff:
    controller: "Se3Controller"

min_height_checking:

  enabled: true
  rate: 10 # [Hz]
  min_height: 1.5 # [m]
  safety_height_offset: 1.0 # how much higher to ascend above the min height
